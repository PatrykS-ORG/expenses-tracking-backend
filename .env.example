# Copy to .env and fill in real values. Never commit .env.

# --- Server ---
PORT=3000

# --- Supabase PostgreSQL (Prisma) ---
# Pooled connection URL (runtime queries)
DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
# Direct connection (migrations) — required for prisma migrate
DIRECT_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

# --- Supabase Auth (JWT verification) ---
# Required for asymmetric signing (ES256 / JWKS) — default on new Supabase projects
SUPABASE_URL=https://your-project.supabase.co
# Legacy HS256 only — not used when SUPABASE_URL is set and JWKS is available
SUPABASE_JWT_SECRET=your-supabase-jwt-secret

# --- AI (DeepSeek) ---
DEEPSEEK_API_KEY=

# --- Planned: Email (Brevo SMTP) ---
# BREVO_SMTP_HOST=smtp-relay.brevo.com
# BREVO_SMTP_PORT=587
# BREVO_SMTP_USER=
# BREVO_SMTP_PASSWORD=
# BREVO_FROM_EMAIL=noreply@example.com

# --- Planned: Nextcloud WebDAV (shared server credentials) ---
# NEXTCLOUD_WEBDAV_URL=https://nextcloud.example.com/remote.php/dav/files/
# NEXTCLOUD_USERNAME=
# NEXTCLOUD_PASSWORD=

# --- Planned: Cron webhook ---
# CRON_SECRET=generate-a-long-random-secret
