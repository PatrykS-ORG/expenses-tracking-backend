/** Fallback when AI_TOKENS_PER_CREDIT is unset or invalid. */
export const TOKENS_PER_CREDIT = 1000;

/** Fallback when AI_MONTHLY_CREDIT_LIMIT is unset or invalid. */
export const DEFAULT_MONTHLY_AI_CREDIT_LIMIT = 50;

export const AI_TOKENS_PER_CREDIT_ENV = 'AI_TOKENS_PER_CREDIT';
export const AI_MONTHLY_CREDIT_LIMIT_ENV = 'AI_MONTHLY_CREDIT_LIMIT';
